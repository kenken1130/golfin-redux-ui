using UnityEngine;

/// <summary>
/// Logo screen — black background with centered GOLFIN logo.
/// No localized text. Auto-advances via GameBootstrap timing.
/// </summary>
public class LogoScreen : ScreenBase
{
    // Logo is set up in the scene hierarchy (Image components)
    // No runtime logic needed — purely visual
    // GameBootstrap controls when to advance
}
